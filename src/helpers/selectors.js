export const selectCurrentAdvert = state => state.modal.currentAdvert;

export const selectAdverts = state => state.adverts.adverts;

export const selectModalOpen = state => state.modal.isOpen;

export const selectFavoriteAdverts = state => state.adverts.favoriteAdverts;

export const selectCurrentPage = state => state.adverts.currentPage;
